from math import inf
def divide(first, second):
    if second != 0:
        res = first / second
        return print(res)
    else:
        return print(inf)
# divide(49, 7)
# divide(15, 0)
